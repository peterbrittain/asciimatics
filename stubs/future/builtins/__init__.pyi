import builtins
from future import utils as utils
from future.builtins.iterators import filter as filter, map as map, zip as zip
from future.builtins.misc import ascii as ascii, chr as chr, hex as hex, input as input, isinstance as isinstance, max as max, min as min, next as next, oct as oct, open as open, pow as pow, round as round, super as super
from future.utils import PY3 as PY3

bytes = builtins.bytes
dict = builtins.dict
int = builtins.int
list = builtins.list
object = builtins.object
range = builtins.range
str = builtins.str
